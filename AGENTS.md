# AGENTS.md

Read `README.md` first.

## Repository Guidance

- This is a static GitHub Pages site rooted at `index.html`.
- Presentation assets live under `stylesheets/` and `javascripts/`.
- `data/pages-sites.json` is maintained by `scripts/update_pages_sites.py`; keep generated data deterministic and avoid hand-editing it unless the workflow itself changes.
